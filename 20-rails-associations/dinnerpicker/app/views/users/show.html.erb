<h1><%= @user.name %>'s Dinners:</h1>

<ul>
	<% @user.dinners.each do |dinner| %>
		<li><%= dinner.name %></li>
	<% end %>
</ul>

<%#= link_to "Delete", @user, method: :delete, class: "btn btn-custom" %>

<%= button_to "Delete User", @user, method: :delete, class: 'btn btn-danger' %>