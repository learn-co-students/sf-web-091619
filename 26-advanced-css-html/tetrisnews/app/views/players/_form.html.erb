<%= form_for @player do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>

	<%= f.label :score %>
	<%= f.text_field :score %>

	<%= f.label :image %>
	<%= f.text_field :image %>

	<%= f.submit "Create Player" %>
<% end %>